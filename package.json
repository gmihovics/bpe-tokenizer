{
  "name": "bpe-tokenizer-playground",
  "version": "1.0.0",
  "description": "A single-page app that visualizes each step of Byte-Pair Encoding tokenization.",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "serve": "python3 -m http.server 8000"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
